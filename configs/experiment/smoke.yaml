# @package _global_

defaults:
  - override /model: small
  - override /training: disabled
  - _self_

seed: 42
job_name: smoke
mode: simulate

stimulus:
  kind: drifting_grating
  resolution: 36
  n_theta: 4
  visual_gain: 40.0

simulation:
  duration_tau_e: 2.0

solver:
  transfer:
    mu_tab_max: 2.0
