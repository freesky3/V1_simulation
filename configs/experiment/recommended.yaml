# @package _global_

defaults:
  - override /model: default
  - override /solver: scipy_rk4
  - override /background: none
  - override /training: disabled
  - _self_

seed: 42
job_name: recommended
mode: simulate
